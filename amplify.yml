version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@9
            - pnpm install
            - pnpm --filter @gam/db generate
        build:
          commands:
            - pnpm --filter @gam/web build
            - cp -r .next/static .next/standalone/apps/web/.next/static
            - cp -r public .next/standalone/apps/web/public
            - cp -r .next/standalone/node_modules/. .next/standalone/apps/web/node_modules/
      artifacts:
        baseDirectory: .next/standalone/apps/web
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*
    appRoot: apps/web